# Cloud TPU v4 Pod substrate (16 v4 TPUs / TPU v4-16 pod, 32 TensorCores).
# Hardware target: Google Cloud TPU v4 pod slice with JAX / XLA / HLO accelerated kernels.
--find-links https://storage.googleapis.com/jax-releases/libtpu_releases.html
jax[tpu]
torch
numpy>=1.24
matplotlib>=3.8
pytest>=8.0
