# Falsifiable claim ledger

This ledger translates the theory into tests whose purpose is to find failures. “Expected” never means assumed true; it is the prediction that the listed experiment must attack. Claim wording reflects the corrected manuscript and the Phase 6–7 failure studies.

| claim_id | theoretical_claim | experiment_that_can_falsify_it | metric | expected_behavior | failure_interpretation | manuscript_section |
|---|---|---|---|---|---|---|
| CSM-ALG-001 | The state update is exactly `S_t=lambda_t S_(t-1)+beta_t k_t k_t^T`, `C_t=lambda_t C_(t-1)+beta_t v_t k_t^T`. | Compare an fp64 sequential recurrence with an independently coded sum over all suffix-decayed historical writes for random dimensions, beta, lambda, and seeds. | Max absolute/relative errors in S, C, reads, and uncertainty. | Agreement at fp64 roundoff in well-conditioned cases. | Implementation does not represent Definition 5.1 or the recomputation is not independent. | Definition 5.1 |
| CSM-ALG-002 | Nonnegative beta and lambda preserve positive semidefiniteness of S. | Check symmetry and minimum eigenvalue after random and pathological sequences; formally prove rank-one PSD and update closure in Lean. | Symmetry residual and `lambda_min(S)`. | Residual near roundoff; minimum eigenvalue no lower than a scale-aware negative tolerance. | Recurrence/sign bug, invalid negative weights, or eigensolver resolution issue requiring diagnosis. | Definition 5.1 |
| CSM-REC-001 | Independent keys up to `K<=d_k` interpolate stored values as epsilon tends to zero, with finite-epsilon error controlled by conditioning. | Sweep epsilon over independent key sets with controlled Gram spectra; query every stored key. | Maximum/mean recall L2 error versus epsilon and theorem bound. | Error tends to zero; ill-conditioning slows convergence according to the bound. | Theorem 5.2, implementation, or stated assumptions are wrong. | Theorem 5.2 |
| CSM-REC-002 | Solver reads remove crosstalk for overlapping independent keys while strictly-positive finite-score similarity smoothing leaks. | Construct overlapping keys and compare CSM with finite-temperature similarity softmax at stored queries. | Stored-key error versus overlap, epsilon, and temperature. | CSM approaches zero with epsilon; finite-score smoother retains nonzero leakage. | Scoped separation does not hold, or comparison violated its scoring assumptions. | Proposition 5.5(a), corrected |
| CSM-REC-003 | Signed linear-functional queries are exact in the key span as epsilon tends to zero; a single convex smoother is hull-bound. | Query random signed combinations of independent keys and compare with the same value combination and its distance from the value convex hull. | Functional L2 error; convex-hull distance lower bound. | CSM error tends to zero; convex read cannot beat hull distance. | Linear-functional clause or geometric comparison is wrong. | Definition 2.5; Proposition 5.5(b) |
| CSM-STAT-001 | Repeated equal-precision noisy observations are averaged at risk `sigma^2 d_v/n`. | Monte Carlo repeated writes of one key over logarithmic n. | Empirical MSE, predicted MSE, ratio, fitted log-log slope. | Ratio near one and slope near -1 within sampling error. | Ridge/noise derivation, implementation, or simulation is wrong. | Theorem 5.4(c) |
| CSM-STAT-002 | Unequal beta implements precision weighting; an evidence-blind identical-key smoother is suboptimal unless all precisions match. | Simulate heteroscedastic identical-key observations; compare beta-weighted and uniform means. | Per-coordinate empirical variance and theoretical variance. | Weighted variance `1/sum(beta)`; uniform variance larger for unequal beta. | Beta lacks precision semantics, or corrected Proposition 5.5(c) is wrong. | Proposition 4.6(b); Proposition 5.5(c), corrected |
| CSM-STAT-003 | The read is discounted ridge regression and obeys the stated bias-variance decomposition under the linear-Gaussian model. | Generate W, keys, and Gaussian values; compare empirical risk with analytic bias and variance over spectrum/load sweeps. | Risk decomposition residual and confidence-bound coverage. | Empirical risk matches analytic terms within Monte Carlo uncertainty. | Algebra, model implementation, or theorem is wrong; outside-model data cannot test the in-model theorem. | Theorem 5.7 |
| CSM-UNC-001 | `c_t(q)=q^T A_t^-1 q` is posterior epistemic variance per coordinate; on an unwritten direction it is `||q||^2/epsilon`. | Compare solve output to inverse oracle and Monte Carlo posterior samples; test non-unit unwritten queries. | Absolute error, calibration curve, interval coverage. | Equality in fp64; calibrated coverage under the stated model. | Solve/derivation is wrong or generative assumptions are violated. | Proposition 5.6, corrected |
| CSM-UNC-002 | With lambda=1, adding PSD evidence cannot increase c(q). | Add writes progressively and query fixed directions, including orthogonal controls. | Stepwise delta in c(q). | Non-increasing along all queries, unchanged for exactly orthogonal evidence. | Loewner-order prediction or implementation is wrong. | Proposition 5.6 |
| CSM-HOP-001 | H chained reads have error at most `epsilon_1 sum_(j<H) L^j`. | Functional graphs with controlled operator norm, merges, and repeated successors; compare actual hop error with bound. | End-to-end L2 error, bound ratio, measured L. | Error never exceeds bound within numerical tolerance. | Perturbation proof or per-hop error estimate is wrong. | Theorem 5.8 |
| CSM-HOP-002 | The simpler `H epsilon_1` bound requires L<=1; normalized value columns alone do not ensure contraction. | Use orthonormal keys with repeated normalized successor columns and compute the read-operator norm. | Spectral norm L. | Counterexample gives `sqrt(2)/(1+epsilon)>1` for small epsilon; contractive cases obey simplified bound. | If normalization alone appears sufficient, the counterexample/test is wrong. | Theorem 5.8, corrected |
| CSM-GATE-001 | beta=0 skips the current evidence; beta scales evidence precision. | Closed-form single/duplicate writes including zero and extreme beta. | State delta, read, variance. | No write increment at beta=0; weighted closed forms elsewhere. | Gate semantics or implementation is wrong. | Definition 5.1; Proposition 4.6(b) |
| CSM-GATE-002 | lambda produces suffix-product forgetting exactly. | Impulse writes followed by controlled lambda schedules; compare against analytic suffix products. | State/read coefficient error. | Coefficients equal products of later lambdas to roundoff. | Recurrence indexing/order is wrong. | Definition 5.1; Theorem 4.1 |
| CSM-LEARN-001 | A learned ridge memory requires keys and queries to use a compatible operator coordinate chart; a mismatched query adds an error controlled by the ridge read-operator norm. | Train the seven Phase 6 tasks with a shared chart, then restore independent normalized key/query encoders in a held-out ablation; formally verify the scalar error decomposition in Lean. | Task risk/success, query mismatch, spectrum, effective rank, and exact scalar decomposition. | Shared-chart CSM solves every task and beats frozen random features across seeds; the independent chart may fail despite nonzero rank. | Failure of an independent chart does not refute ridge retrieval; failure of the shared chart would refute the learning claim in the tested model class. | Definition 5.1; Lemma 5.2a |
| CSM-GATE-003 | When beta weights both sufficient statistics, observable reliability cues can learn useful relative evidence precision. | Mix clean, noisy, corrupt, and irrelevant observations; compare learned beta with beta=1, a cross-statistic-only generic gate, and oracle precision. | Downstream risk, category ordering, and oracle-weight correlation by seed. | Learned beta improves fixed beta and orders clean > noisy > corrupt/distractor across seeds. | The precision interpretation is not learned from the supplied cues, or the model exploits an unintended scale/decoder interaction. | Proposition 4.6(b); Definition 5.1 |
| CSM-GATE-004 | Lambda is a forgetting action conditional on drift evidence, not by itself a Bayesian hidden-changepoint detector. A token-emitted observable-cue lambda remains scan-compatible; a state-dependent innovation detector does not preserve the simple affine scan. | Compare fixed lambdas, a noisy observable-cue gate, an innovation-only ablation, and oracle changepoints; then train beta/lambda jointly only if separate gates pass. | Risk, post-change risk, stationary/change lambda, drift correlation, and joint semantic ordering. | Observable-cue lambda improves fixed tradeoffs and reacts to drift; innovation alone may be insufficient. | If hidden drift must be inferred, add and account for detector state (for example a run-length posterior); do not attribute detection to lambda or claim the original affine scan. | Proposition 4.6; Section 4.5 |
| CSM-SCAN-001 | Affine elements combine associatively and reproduce sequential recurrence states. | Lean proof plus randomized three-way parenthesization and long scans. | Lean kernel acceptance; tensor max error. | Formal build succeeds and numerical paths agree. | Faithful theorem false, encoding wrong, or implementation bug; build failure alone is not a refutation. | Theorem 4.1; Proposition 6.1 |
| CSM-SOLVE-001 | For epsilon>0, `A=S+epsilon I` is positive definite and reads can use Cholesky/solve without an inverse. | Lean proof of positive definiteness/invertibility; compare Cholesky and solve against tiny inverse oracle. | Factorization success and read/uncertainty errors. | Stable agreement in fp64. | PSD assumptions violated or implementation/numerics wrong. | Definition 5.1 |
| CSM-NUM-001 | Finite-precision error grows with conditioning and is controlled by epsilon. | Prescribe key singular spectra; compare fp32/bf16/fp16 reads to fp64 over epsilon. | Relative error versus kappa and dtype unit roundoff. | Error trend grows with kappa; regularization caps it. | Numerical prediction is too strong or kernel behavior differs. | Theorem 6.6 (future gate) |
| CSM-NUM-002 | The scaled Cholesky floor tracks epsilon I in the stated constant-lambda regimes. | Long-run coordinate-cycling floor simulation versus closed-form interval. | Min/max diagonal floor ratio and factorization failures. | Ratios lie in Lemma 6.4 intervals after burn-in. | Lemma/indexing is wrong; this is beyond Phase 1 implementation. | Lemma 6.4 (future gate) |
| CSM-CAP-001 | A value-linear memory with `p_v=d_k d_v` cannot exactly recall more than d_k arbitrary associations and CSM reaches d_k independent keys. | Rank tests below/at/above capacity over adversarial key/value assignments. | Worst-case recall error and effective map rank. | At-capacity interpolation; unavoidable assignments fail above capacity. | Converse premises, experiment coverage, or theorem is wrong. | Theorems 3.3 and 5.9 |
| CSM-LOW-001 | Randomized exact block recall with error delta requires at least `(1-H2(delta)) Kb` state bits. | Exhaustive tiny-state enumeration/random-access-code search for small K,b and delta. | Minimum state bits found. | No counterexample below lower bound. | Corrected bitwise-Fano theorem or enumeration is wrong. | Theorem 3.1(b), corrected |

## Gate rule

**NO NLP SCALE EXPERIMENT is allowed until the synthetic-memory gate and the learned-memory gate pass.** Both prerequisite gates now pass through Phase 7; no NLP experiment has yet been run or authorized.
